# =============================================================================
# Environment Variables for Blog CMS
# =============================================================================
# Copy this file to .env and fill in your actual values
#
# To load these in Windows Command Prompt, run each line manually:
#   set EMAIL_HOST_USER=your-email@gmail.com
#   set EMAIL_HOST_PASSWORD=your-16-char-app-password
#
# Or create a batch file (.bat) to set them all at once.
# =============================================================================

# ----- Gmail SMTP Configuration -----
# To get an App Password:
# 1. Go to https://myaccount.google.com/
# 2. Enable 2-Factor Authentication (if not already enabled)
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Select "Mail" and your device, then generate
# 5. Copy the 16-character password (no spaces)

EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ----- Django Secret Key -----
# Generate a new one at: https://djecrety.ir/
SECRET_KEY=your-secret-key-here

# ----- Debug Mode -----
# Set to False in production!
DEBUG=True
